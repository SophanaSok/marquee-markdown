//! `mmd`: the short name for `marquee-markdown`.
//!
//! Identical in every way — it is the same program under a name that is
//! quicker to type for something invoked constantly.

fn main() -> std::process::ExitCode {
    marquee_markdown::cli::run::main()
}
